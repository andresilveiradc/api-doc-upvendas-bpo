info:
  name: DETALHAR CONTA BANCARIA
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_servidor}}/contas-bancarias/:idcontabancaria"
  params:
    - name: idcontabancaria
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{token_auth}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        let authHeader = res.getHeader("authorization");
        if (authHeader) {
            bru.setEnvVar("token_auth", authHeader.replace(/^Bearer\s+/i, ""));
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
