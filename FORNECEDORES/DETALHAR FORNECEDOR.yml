info:
  name: DETALHAR FORNECEDOR
  type: http
  seq: 3

http:
  method: GET
  url: "{{url_servidor}}/fornecedores/:idpessoa"
  params:
    - name: idpessoa
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{token_auth}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        let authHeader = res.getHeader("authorization");
        if (authHeader) {
            bru.setEnvVar("token_auth", authHeader.replace(/^Bearer\s+/i, ""));
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
